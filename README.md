# AlfrEXco

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add `alfrEXco` to your list of dependencies in `mix.exs`:

    ```elixir
    def deps do
      [{:alfrEXco, "~> 0.1.0"}]
    end
    ```

  2. Ensure `alfrEXco` is started before your application:

    ```elixir
    def application do
      [applications: [:alfrEXco]]
    end
    ```

# alfrEXco
